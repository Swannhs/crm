apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  labels:
    app.kubernetes.io/name: billing-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: billing-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: billing-service
    spec:
      containers:
        - name: app
          image: ghcr.io/mymanager/billing-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 7020
              name: http
          env:
            - name: PORT
              value: "7020"
            - name: KAFKA_BROKERS
              value: kafka:9092
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: billing-service-secret
                  key: DB_URL
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 512Mi

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mymanager-prod

resources:
  - namespace.yaml
  - ../../base
  - ingress.yaml

images:
  - name: ghcr.io/mymanager/organization-service
    newTag: stable

patches:
  - path: replicas-patch.yaml

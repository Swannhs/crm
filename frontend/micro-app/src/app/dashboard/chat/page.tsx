import { ChatView } from 'src/sections/chat/view/chat-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Chat - Dashboard` };

export default function Page() {
  return <ChatView />;
}

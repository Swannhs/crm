import { WebhookListView } from 'src/sections/omnichannel/webhooks/view/webhook-list-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Omnichannel Webhooks - Dashboard` };

export default function Page() {
  return <WebhookListView />;
}

import { ContactListView } from 'src/sections/contacts/view/contact-list-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Contacts',
};

export default function Page() {
  return <ContactListView />;
}

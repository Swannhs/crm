import { PublicFlowWorkspaceView } from 'src/sections/public/public-flow-workspace-view';

export const metadata = {
  title: 'Menu',
};

export default function Page() {
  return <PublicFlowWorkspaceView mode="mobile-menu" />;
}

# name: CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   lint:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '22'
#           cache: 'yarn'
#       - name: Install dependencies
#         run: yarn install --frozen-lockfile
#       - name: Run lint
#         run: yarn workspaces run lint --if-present

#   typecheck:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '22'
#           cache: 'yarn'
#       - name: Install dependencies
#         run: yarn install --frozen-lockfile
#       - name: Run typecheck
#         run: yarn workspaces run typecheck --if-present

#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '22'
#           cache: 'yarn'
#       - name: Install dependencies
#         run: yarn install --frozen-lockfile
#       - name: Run tests
#         run: yarn workspaces run test --if-present

#   build:
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         service: [
#           api-router-service,
#           odoo-integration-service,
#           organization-service,
#           realtime-service,
#           booking-service,
#           integrations-service,
#           email-sync-service
#         ]
#     steps:
#       - uses: actions/checkout@v4
#       - name: Build Docker image
#         run: |
#           if [ -f "services/${{ matrix.service }}/docker/Dockerfile.prod" ]; then
#             docker build -t mymanager/${{ matrix.service }}:latest -f services/${{ matrix.service }}/docker/Dockerfile.prod services/${{ matrix.service }}
#           elif [ -f "services/${{ matrix.service }}/Dockerfile" ]; then
#             docker build -t mymanager/${{ matrix.service }}:latest -f services/${{ matrix.service }}/Dockerfile services/${{ matrix.service }}
#           else
#             echo "No Dockerfile found for ${{ matrix.service }}, skipping build."
#           fi
